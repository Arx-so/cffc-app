import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  titleRow: {
    flexDirection: "row",
    alignItems: "center",
    gap: 4,
  },
  titleIcon: {
    width: 22,
    height: 22,
  },
  title: {
    fontWeight: "bold",
  },
  navigation: {
    backgroundColor: "transparent",
  },
});
